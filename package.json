{
  "name": "draftmaid",
  "private": true,
  "scripts": {
    "test": "node --test tests/engine.test.js && playwright test",
    "test:unit": "node --test tests/engine.test.js",
    "test:e2e": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
